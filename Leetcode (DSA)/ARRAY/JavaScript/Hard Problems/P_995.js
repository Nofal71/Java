/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
var minKBitFlips = function (nums, k) {
    let flipCount = 0, index = 0;

   for(let i = 0 ; i < nums.length ; i++){
    
   }

    return flipCount;
};

let nums = [1, 0, 0], k = 2
console.log(minKBitFlips(nums, k));